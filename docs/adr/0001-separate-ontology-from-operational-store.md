---
status: accepted
---

# 将最小人才本体与运营数据存储解耦

MVP采用版本化本体词表和语义标准化插件定义技能、岗位、行业及证据，SQLite只保存运营快照、规范概念ID、映射证据、匹配和反馈。这样既能让当前规则算法与未来LLM共享同一语义契约，又避免为12个岗位、1000名模拟人员提前引入RDF仓库或图数据库；若以后需要SPARQL、OWL推理或大规模多跳关系，只替换本体存储/推理插件，不改变Golden Flow和业务数据契约。

