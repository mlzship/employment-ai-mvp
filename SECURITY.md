# 安全边界

本仓库是单实例 MVP 验证工程，不应直接作为公网生产招聘系统使用。

- 仓库不包含真实个人信息、SSH 私钥、GitHub Token 或生产密码；提交前由 `.gitignore` 阻断常见凭据文件。
- 生产模式会拒绝示例 `SESSION_SECRET`、示例密码和 `AUTH_DISABLED=true`。
- Excel 上传限制为 `.xlsx`、10MB 压缩体积、80MB 解压体积，并检查异常压缩比、加密包、路径穿越和公式单元格。
- 姓名、身份证号和手机号不在数据契约中，也不作为匹配特征。
- 大模型密钥只从 `LLM_API_KEY` 环境变量读取，状态接口、前端、数据库和审计日志均不返回明文密钥。
- 模型调用只发送匿名候选 ID 与岗位相关字段，不发送姓名、证件、电话、村镇位置或特殊标签；真实数据接入前仍需完成数据委托处理与出境评审。
- 大模型响应必须通过 JSON、候选集合、重复 ID、分数范围和长度校验；失败时不覆盖已有匹配结果。
- AI只做排序和解释；审核、触达和录用均由人工或外部受控流程完成。
- Compose 默认只监听 `127.0.0.1`。公网部署前必须增加 HTTPS 反向代理、强身份认证、速率限制、日志脱敏、备份恢复和安全评审；生产模型端点必须使用 HTTPS。

如发现漏洞，请通过私有渠道报告，不要把凭据或个人数据写入公开 Issue。
